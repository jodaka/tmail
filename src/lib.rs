//! Post — a Gmail-inspired, keyboard-first terminal email client backed by
//! the Himalaya CLI. Phase 1: application shell and deterministic state core
//! (mock data only, no backend calls).

pub mod app;
pub mod domain;
pub mod input;
pub mod runtime;
pub mod ui;
