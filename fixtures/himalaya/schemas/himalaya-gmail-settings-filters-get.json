{
  "$defs": {
    "GmailFilter": {
      "description": "Mail filter of a Gmail account.",
      "properties": {
        "action": {
          "anyOf": [
            {
              "$ref": "#/$defs/GmailFilterAction"
            },
            {
              "type": "null"
            }
          ],
          "description": "Action applied to messages matching the criteria."
        },
        "criteria": {
          "anyOf": [
            {
              "$ref": "#/$defs/GmailFilterCriteria"
            },
            {
              "type": "null"
            }
          ],
          "description": "Matching criteria of the filter."
        },
        "id": {
          "description": "Server-assigned identifier of the filter.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "GmailFilterAction": {
      "description": "Action applied by a Gmail filter to a matching message.",
      "properties": {
        "addLabelIds": {
          "description": "Identifiers of labels to add to the message.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "forward": {
          "description": "Email address the message is forwarded to.",
          "type": [
            "string",
            "null"
          ]
        },
        "removeLabelIds": {
          "description": "Identifiers of labels to remove from the message.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "GmailFilterCriteria": {
      "description": "Conditions matched by a Gmail filter against incoming messages.",
      "properties": {
        "excludeChats": {
          "description": "Whether chat messages are excluded from matching.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "from": {
          "description": "Sender display name or email address to match.",
          "type": [
            "string",
            "null"
          ]
        },
        "hasAttachment": {
          "description": "Whether the message must have an attachment.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "negatedQuery": {
          "description": "Gmail search query the message must not match.",
          "type": [
            "string",
            "null"
          ]
        },
        "query": {
          "description": "Gmail search query the message must match.",
          "type": [
            "string",
            "null"
          ]
        },
        "size": {
          "description": "Message size in bytes compared with `size_comparison`.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "sizeComparison": {
          "anyOf": [
            {
              "$ref": "#/$defs/GmailFilterSizeComparison"
            },
            {
              "type": "null"
            }
          ],
          "description": "How the message size relates to the `size` field."
        },
        "subject": {
          "description": "Case-insensitive phrase matched in the message subject.",
          "type": [
            "string",
            "null"
          ]
        },
        "to": {
          "description": "Recipient display name or email address to match.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "GmailFilterSizeComparison": {
      "description": "Comparison applied to the message size in a filter criterion.",
      "oneOf": [
        {
          "const": "unspecified",
          "description": "No size comparison specified.",
          "type": "string"
        },
        {
          "const": "smaller",
          "description": "Matches messages smaller than the given size.",
          "type": "string"
        },
        {
          "const": "larger",
          "description": "Matches messages larger than the given size.",
          "type": "string"
        }
      ]
    }
  },
  "$ref": "#/$defs/GmailFilter",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "A Gmail filter, rendered as a one-line summary of its criteria and\naction or, under `--json`, as the filter resource itself instead of a\nwrapped human string.\n\nThe resource is emitted verbatim so that one filter read with `get`\nhas the very same shape as a row of `list`, and so that the criteria\nand action stay machine-readable: the summaries the text rendering\nbuilds are lossy on purpose.",
  "title": "GmailSettingsFilterGetOutput"
}