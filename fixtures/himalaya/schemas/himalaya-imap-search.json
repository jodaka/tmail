{
  "$defs": {
    "SearchResult": {
      "description": "One row of the SEARCH results table: a single message id.",
      "properties": {
        "id": {
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Renderable table of SEARCH result message ids.",
  "properties": {
    "ids": {
      "items": {
        "$ref": "#/$defs/SearchResult"
      },
      "type": "array"
    },
    "uid_mode": {
      "type": "boolean"
    }
  },
  "required": [
    "uid_mode",
    "ids"
  ],
  "title": "SearchTable",
  "type": "object"
}